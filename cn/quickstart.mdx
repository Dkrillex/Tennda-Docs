---
title: '快速开始'
description: '只需简单三步，即可开始使用 TenndaAI 访问各种 AI 模型'
---

## 第一步：账户准备

### 1.1 开通账号

<Note>
TenndaAI **不提供对外注册**。如需使用服务，请联系我们的 BD 团队为您开通账号。
</Note>

请通过以下方式联系 BD 开通账号：

<CardGroup cols={2}>
  <Card title="电话联系 BD" icon="phone" href="tel:+8615113062111">
    拨打电话：**15113062111**
  </Card>
  <Card title="邮件联系 BD" icon="envelope" href="mailto:bd@tennda.ai">
    发送邮件至 bd@tennda.ai
  </Card>
</CardGroup>

账号开通后，您将收到登录信息，可访问 [TenndaAI 控制台](https://client.tennda.ai) 登录使用。

### 1.2 账户充值

登录控制台后进行充值：
1. 点击「个人中心」
2. 点击「费用充值」，完成支付（支持支付宝、微信）

如需开具发票或对公转账，请联系 BD 协助处理。

## 第二步：创建 API 密钥

### 2.1 生成密钥

1. 在后台导航点击「模型中心」
2. 点击「API 密钥」
3. 点击「新建」生成密钥


## 第三步：开始调用

### 3.1 获取接入信息

| 配置项 | 值 |
|-------|-----|
| **API 地址（Base URL）** | `https://api.tennda.ai/v1` |
| **API 密钥** | 您创建的密钥 |
| **请求格式** | 完全兼容 OpenAI API |

### 3.2 测试调用

使用 curl 快速测试：

```bash
curl https://api.tennda.ai/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '{
    "model": "gpt-5.4",
    "messages": [{"role": "user", "content": "Hello!"}]
  }'
```

### 3.3 代码示例

<Tabs>
  <Tab title="Python">
    ```python
    from openai import OpenAI

    client = OpenAI(
        api_key="YOUR_API_KEY",
        base_url="https://api.tennda.ai/v1"
    )

    response = client.chat.completions.create(
        model="gpt-5.4",
        messages=[
            {"role": "user", "content": "Hello!"}
        ]
    )

    print(response.choices[0].message.content)
    ```
  </Tab>
  <Tab title="Node.js">
    ```javascript
    import OpenAI from 'openai';

    const openai = new OpenAI({
      apiKey: 'YOUR_API_KEY',
      baseURL: 'https://api.tennda.ai/v1'
    });

    const response = await openai.chat.completions.create({
      model: 'gpt-5.4',
      messages: [{ role: 'user', content: 'Hello!' }]
    });

    console.log(response.choices[0].message.content);
    ```
  </Tab>
  <Tab title="Java">
    ```java
    OpenAiService service = new OpenAiService(
        "YOUR_API_KEY", 
        Duration.ofSeconds(60), 
        "https://api.tennda.ai/v1"
    );

    ChatCompletionRequest request = ChatCompletionRequest.builder()
        .model("gpt-5.4")
        .messages(List.of(
            new ChatMessage(ChatMessageRole.USER, "Hello!")
        ))
        .build();

    ChatCompletionResult result = service.createChatCompletion(request);
    System.out.println(result.getChoices().get(0).getMessage().getContent());
    ```
  </Tab>
</Tabs>

## 下一步

恭喜！您已经成功完成了 TenndaAI 的接入。接下来您可以：

<Columns cols={2}>
  <Card title="查看 API 文档" icon="book" href="/cn/api-reference/models">
    了解完整的 API 接口说明
  </Card>
  <Card title="集成到应用" icon="puzzle-piece" href="/cn/integrations/cursor">
    将 TenndaAI 集成到各种工具
  </Card>
</Columns>

## 常见问题

<AccordionGroup>
  <Accordion title="如何切换模型？">
    只需修改请求中的 `model` 参数：
    ```json
    {
      "model": "gpt-5.4",
      "model": "claude-sonnet-4-6",
      "model": "gemini-3.1-pro-preview"
    }
    ```
  </Accordion>
  <Accordion title="支持哪些编程语言？">
    TenndaAI 兼容 OpenAI API 标准，支持所有 OpenAI SDK 支持的语言：
    - Python
    - JavaScript/TypeScript
    - Java
    - C#/.NET
    - Go
    - Ruby
    - PHP
  </Accordion>
  <Accordion title="如何查看余额？">
    登录控制台即可查看：
    - 账户余额
    - 使用记录
    - 消费统计
  </Accordion>
  <Accordion title="如何开通账号？">
    TenndaAI 不提供对外注册，账号统一由 BD 团队为您开通。开通后您将享受到：
    - 专属 BD 一对一对接服务
    - 定制化的模型方案与配额支持
    - 更及时的技术支持与问题响应

    如需开通，请发送邮件至 bd@tennda.ai 或致电 15113062111。
  </Accordion>
  <Accordion title="遇到问题怎么办？">
    1. 查看 API 文档
    2. 检查常见错误
    3. 联系客服：bd@tennda.ai
  </Accordion>
</AccordionGroup>

<Tip>
保存好您的 API 密钥，并定期在控制台查看使用日志，合理优化成本。祝使用愉快~
</Tip>
